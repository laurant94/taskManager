<div class="row text-center">
  <h1>Editing Task</h1>

  <%= render 'form', user: @user %>
  <%= link_to 'Show', admin_user_path(@user), class: 'text-primary' %> 
  <%= link_to 'Back', admin_users_path, class: 'text-success' %>
</div>
